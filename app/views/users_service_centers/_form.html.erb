<%= form_for(@users_service_center) do |f| %>
  <% if @users_service_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_service_center.errors.count, "error") %> prohibited this users_service_center from being saved:</h2>

      <ul>
      <% @users_service_center.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
  <%= f.text_field :service_center_id %>
    <%= f.submit %>
  </div>
<% end %>
