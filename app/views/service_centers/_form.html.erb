<%= form_for(@service_center) do |f| %>
  <% if @service_center.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_center.errors.count, "error") %> prohibited this service_center from being saved:</h2>

      <ul>
      <% @service_center.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
