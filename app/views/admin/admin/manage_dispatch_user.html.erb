    <div class="container">

    <div class="serch-fld">
          <label class="margin-top">Dashboard </label>
           <span class="align-right">
              <input class="form-control" type="search">
                <input value="" class="search-submit" type="submit" />
            </span>
           
        </div>  
        <ol class="breadcrumb">
      <li class="active"> Main</li>
      </ol>
       <% if flash[:notice] %>
            <p style="color:#fff;"><%= flash[:notice] %></p>
        <% end %><a href="/admin/manage_dispatch_user_edit">Add New</a>
        <div class="table-responsive">
          <table class="table-striped table-bordered table-condensed" width="100%">
            <% @users.each do |usr| %>
           <tr><td>
              <a style="color:#000" href="manage_dispatch_user_edit?id=<%= usr.id %>"><%= usr.FirstName %>&nbsp;<%= usr.LastName %></a>
              </td></tr>
            <% end %>
            </table>
            </div>
        </div>
      </div>

