<%= form_for(@service_alert) do |f| %>
  <% if @service_alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_alert.errors.count, "error") %> prohibited this service_alert from being saved:</h2>

      <ul>
      <% @service_alert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
