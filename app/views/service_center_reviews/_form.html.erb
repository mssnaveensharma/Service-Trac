<%= form_for(@service_center_review) do |f| %>
  <% if @service_center_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_center_review.errors.count, "error") %> prohibited this service_center_review from being saved:</h2>

      <ul>
      <% @service_center_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
