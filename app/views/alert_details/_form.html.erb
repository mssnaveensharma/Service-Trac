<%= form_for(@alert_detail) do |f| %>
  <% if @alert_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert_detail.errors.count, "error") %> prohibited this alert_detail from being saved:</h2>

      <ul>
      <% @alert_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
